version: '3'
services:
        web:
                container_name: http-server
                build:
                        context: ./
                ports:
                        - "8888:8888"
                command: ["python3", "main.py", "--app=app_server:app", "--workers=3"]
                privileged: true

